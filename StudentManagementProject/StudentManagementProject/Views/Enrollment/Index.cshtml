@model IEnumerable<StudentManagementProject.Models.Entities.Enrollment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.course_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.student_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.grade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.course_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.student_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.grade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.enrollment_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.enrollment_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.enrollment_id })
        </td>
    </tr>
}

</table>
